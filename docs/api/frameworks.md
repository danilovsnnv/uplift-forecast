# Frameworks

Framework wrappers that retarget a base model's objective while keeping the `UpliftModel`
interface. Currently **RERUM** (rankability-enhanced revenue uplift) wraps a neural model with
a rank-enhanced ZILN revenue loss.

::: uplift_forecast.frameworks
