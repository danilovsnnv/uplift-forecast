# Auto (hyperparameter search)

Optuna-based hyperparameter search with declarative `Suggest*` helpers. Optuna is an optional
dependency (`pip install -e ".[auto]"`) and is imported lazily.

::: uplift_forecast.auto
