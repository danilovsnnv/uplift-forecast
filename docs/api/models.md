# Models

Neural models, meta-learners, and forest models. All implement the shared `UpliftModel`
contract (`fit`, `predict`, `display_name`) and can be combined in a single `UpliftForecast`.

::: uplift_forecast.models
